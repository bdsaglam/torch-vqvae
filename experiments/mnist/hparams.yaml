model:
  input_shape: [1, 28, 28]
  num_hiddens: 128
  num_residual_hiddens: 32
  num_residual_layers: 2
  embedding_dim: 64
  num_embeddings: 512
  commitment_cost: 0.25
  decay: 0.99

# data params
data_dir: "/Users/bdsaglam/torch-datasets"
batch_size: 32
num_workers: 1

# optimizer params
learning_rate: 0.001
weight_decay: 0.0
